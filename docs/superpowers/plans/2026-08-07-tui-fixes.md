# TUI Review Düzeltmeleri — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **Not:** Spec: `docs/superpowers/specs/2026-08-07-tui-fixes-design.md` — başlamadan OKU. Bu plan mevcut TUI implementasyonunun (commit `aaf99d8` sonrası main) üzerine 4 düzeltme uygular. Kod satır numaraları o duruma göredir — eşleşmezse fonksiyon adıyla bul.

**Goal:** Review bulgularını kapat: slug LLM çağrısının CLI oturumunu kirletmesi (B1), kilitli modda çift-Ctrl-C iptal + alt süreç öldürme (B2), sayfa başına güncel genişlik (B3), event-stream sonu guard'ı (B4).

**Architecture:** Davranış sağlamlaştırma — yeni özellik yok. `ask_live` dönüşü `AskOutcome` enum'una genişler; `run_claude_cli`'ye `kill_on_drop`; slug çağrılarından sonra koşulsuz `reset_session()`; genişlik her sayfalamada ölçülür.

**Tech Stack:** Mevcut — Rust, tokio, ratatui, crossterm. Yeni bağımlılık YOK.

## Global Constraints

- Kod yorumları/UI metinleri Türkçe; mevcut stil.
- Plain yol (`ui::is_plain`) davranışı değişmez; mevcut 130 test yeşil kalır.
- Test komutu: `cargo test --quiet`. Commit mesajları Türkçe `alan: özet — gerekçe` stili.
- Her görev: failing test (mümkünse) → implement → tüm testler → commit.

---

### Task 1: B1 — slug çağrısı sonrası `reset_session` (bug fix)

**Files:**
- Modify: `src/backend.rs` (test eklenir)
- Modify: `src/main.rs` (`derive_slug`)
- Modify: `src/tui/run.rs` (slug dalı, `run()` içinde)

**Interfaces:**
- Consumes: mevcut `Backend::reset_session()` (backend.rs:110).
- Produces: değişen imza yok.

- [ ] **Step 1: Failing test — reset session id'yi temizler**

`src/backend.rs` test modülüne:

```rust
    #[test]
    fn reset_session_clears_cli_session_id() {
        let mut b = Backend::Cli { model: "opus".into(), session_id: Some("sid-123".into()) };
        b.reset_session();
        let Backend::Cli { session_id, .. } = &b else { panic!("Cli bekleniyordu") };
        assert!(session_id.is_none());
    }
```

- [ ] **Step 2: Testi çalıştır**

```bash
cargo test --quiet reset_session_clears 2>&1 | tail -3
```

Beklenen: PASS (davranış zaten var — bu test regresyon bekçisi; FAIL ederse `reset_session` bozulmuş demektir, önce onu anla).

- [ ] **Step 3: Plain yol — `derive_slug` içine reset**

`src/main.rs` `derive_slug` (satır ~529) şu hale gelir:

```rust
/// Cümleden konu slug'ını modele çıkart (plain yol). Hata → yerel slug.
/// Çağrı sonrası CLI oturumu KOŞULSUZ sıfırlanır — slug mini-oturumu
/// öğrenme oturumuna resume edilip bağlamı kirletmesin (spec B1).
async fn derive_slug(backend: &mut Backend, raw: &str) -> String {
    let history = [Message::user(raw)];
    let out = match ask_usta(backend, SLUG_SYSTEM, &history).await {
        Ok(reply) => finalize_slug(raw, &reply.text),
        Err(_) => slugify_topic(raw),
    };
    backend.reset_session();
    out
}
```

- [ ] **Step 4: TUI yolu — slug dalına reset**

`src/tui/run.rs` `run()` içindeki slug dalı (satır ~225, `ask_live(... SLUG_SYSTEM ...)` match'i) — match SONUCU bir değişkene alınıp reset eklenir:

```rust
                let slug = match ask_live(
                    &mut tui,
                    &mut editor,
                    &mut events,
                    backend,
                    crate::SLUG_SYSTEM,
                    &[Message::user(raw.as_str())],
                    None,
                )
                .await
                {
                    Ok(reply) => crate::finalize_slug(&raw, &reply.text),
                    Err(_) => crate::slugify_topic(&raw),
                };
                // Slug mini-oturumu öğrenme oturumuna taşınmasın (spec B1).
                backend.reset_session();
                slug
```

NOT: Task 2 `ask_live` dönüşünü `AskOutcome`'a çevirecek — bu görevden sonra buradaki `Ok(reply)` kolu Task 2'de `Ok(AskOutcome::Reply(reply))` olur; reset satırı yerinde kalır.

- [ ] **Step 5: Tüm testler + commit**

```bash
cargo test --quiet 2>&1 | tail -3
git add src/backend.rs src/main.rs src/tui/run.rs
git commit -m "fix: slug LLM çağrısı sonrası reset_session — slug mini-oturumu öğrenme oturumunu kirletmesin"
```

---

### Task 2: B2 — çift Ctrl-C iptal + `kill_on_drop`

**Files:**
- Modify: `src/backend.rs` (`run_claude_cli`)
- Modify: `src/tui/status.rs` (`Status::Thinking`'e `cancel_hint`)
- Modify: `src/tui/run.rs` (`AskOutcome`, `ask_live`, `editor_key_locked`, çağrı yerleri)

**Interfaces:**
- Produces:
  - `pub enum AskOutcome { Reply(crate::backend::Reply), Cancelled }` (run.rs içinde)
  - `ask_live(...) -> Result<AskOutcome>` (imza değişikliği)
  - `Status::Thinking { frame: usize, cancel_hint: bool }` (status.rs — alan eklenir)
  - `enum LockedKey { Edit, CancelRequest }` + `fn classify_locked_key(k: KeyEvent) -> LockedKey` (saf, testlenebilir)
- Consumes: Task 1'in reset satırı (slug dalında yerinde kalır).

- [ ] **Step 1: Failing testler — kilitli tuş sınıflandırması + durum ipucu**

`src/tui/run.rs` sonuna test modülü (yoksa oluştur):

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crossterm::event::{KeyCode, KeyEvent, KeyModifiers};

    #[test]
    fn classify_locked_key_ctrl_c_and_d_are_cancel_requests() {
        let ctrl_c = KeyEvent::new(KeyCode::Char('c'), KeyModifiers::CONTROL);
        let ctrl_d = KeyEvent::new(KeyCode::Char('d'), KeyModifiers::CONTROL);
        assert!(matches!(classify_locked_key(ctrl_c), LockedKey::CancelRequest));
        assert!(matches!(classify_locked_key(ctrl_d), LockedKey::CancelRequest));
    }

    #[test]
    fn classify_locked_key_enter_and_chars_are_edits() {
        assert!(matches!(
            classify_locked_key(KeyEvent::new(KeyCode::Enter, KeyModifiers::NONE)),
            LockedKey::Edit
        ));
        assert!(matches!(
            classify_locked_key(KeyEvent::new(KeyCode::Char('a'), KeyModifiers::NONE)),
            LockedKey::Edit
        ));
    }
}
```

`src/tui/status.rs` test modülüne:

```rust
    #[test]
    fn thinking_with_cancel_hint_shows_hint() {
        let l = render_status(&Status::Thinking { frame: 0, cancel_hint: true }, None, 1_000_000);
        assert!(text(&l).contains("iptal"));
    }
```

- [ ] **Step 2: FAIL doğrula**

```bash
cargo test --quiet classify_locked 2>&1 | tail -3   # derleme hatası: LockedKey tanımsız
cargo test --quiet thinking_with_cancel 2>&1 | tail -3
```

- [ ] **Step 3: `status.rs` — `cancel_hint` alanı**

`Status::Thinking { frame: usize }` → `Status::Thinking { frame: usize, cancel_hint: bool }`. Render'da:

```rust
    if let Status::Thinking { frame, cancel_hint } = s {
        let hint = if *cancel_hint { " (iptal: tekrar Ctrl-C)" } else { "" };
        spans.push(Span::styled(
            format!("{} Usta düşünüyor…{hint} ", FRAMES[frame % FRAMES.len()]),
            Style::default().fg(Color::DarkGray),
        ));
    }
```

Mevcut `thinking_shows_spinner_frame` testi ve `run.rs`'teki tüm `Status::Thinking { frame }` kurulumları `cancel_hint: false` (veya duruma göre) ile güncellenir — derleyici hepsini gösterir.

- [ ] **Step 4: `backend.rs` — `kill_on_drop`**

`run_claude_cli` içinde `cmd` kurulumuna (spawn'dan önce):

```rust
    // Future iptal edilirse (çift Ctrl-C) çocuk süreç öksüz kalmasın.
    cmd.kill_on_drop(true);
```

- [ ] **Step 5: `run.rs` — `AskOutcome` + iptal döngüsü**

`editor_key_locked` yerine saf sınıflandırıcı + yeni `ask_live`:

```rust
/// ask_live sonucu: yanıt geldi ya da kullanıcı çift Ctrl-C ile iptal etti.
pub enum AskOutcome {
    Reply(crate::backend::Reply),
    Cancelled,
}

/// Kilitli moddaki tuşun anlamı — saf, testlenebilir (spec B2).
enum LockedKey {
    /// Editöre işlenecek tuş (Enter dahil — Enter yutulur ama edit sayılır).
    Edit,
    /// Ctrl-C / Ctrl-D — iptal isteği basamağı.
    CancelRequest,
}

fn classify_locked_key(k: KeyEvent) -> LockedKey {
    use crossterm::event::KeyModifiers;
    if k.modifiers.contains(KeyModifiers::CONTROL)
        && matches!(k.code, KeyCode::Char('c') | KeyCode::Char('d'))
    {
        LockedKey::CancelRequest
    } else {
        LockedKey::Edit
    }
}
```

`ask_live` gövdesi (imza aynı kalır, dönüş `Result<AskOutcome>`):

```rust
async fn ask_live(
    tui: &mut Tui,
    editor: &mut InputBox,
    events: &mut EventStream,
    backend: &mut Backend,
    system: &str,
    history: &[Message],
    tokens: Option<u64>,
) -> Result<AskOutcome> {
    let window = backend.context_window();
    let fut = backend.complete(system, history);
    tokio::pin!(fut);
    let mut frame = 0usize;
    let mut cancel_armed = false; // ilk Ctrl-C sonrası true — sayaç sıfırlanmaz (spec B2)
    loop {
        draw(tui, editor, &Status::Thinking { frame, cancel_hint: cancel_armed }, tokens, window)?;
        tokio::select! {
            r = &mut fut => return Ok(AskOutcome::Reply(r?)),
            Some(Ok(ev)) = events.next() => {
                if let Event::Key(k) = ev {
                    match classify_locked_key(k) {
                        LockedKey::CancelRequest if cancel_armed => {
                            // fut düşer → kill_on_drop çocuğu öldürür (backend.rs).
                            return Ok(AskOutcome::Cancelled);
                        }
                        LockedKey::CancelRequest => { cancel_armed = true; }
                        LockedKey::Edit => {
                            if !matches!(k.code, KeyCode::Enter) {
                                let _ = match editor.handle_key(k) {
                                    Action::Exit => Action::None, // buraya düşmez (CancelRequest yakalar) — emniyet
                                    other => other,
                                };
                            }
                        }
                    }
                }
            }
            _ = tokio::time::sleep(std::time::Duration::from_millis(120)) => { frame += 1; }
        }
    }
}
```

Eski `editor_key_locked` ve `draw_locked` silinir (`draw_locked` çağrıları yeni `Status::Thinking {..}` ile `draw`a döner).

- [ ] **Step 6: Çağrı yerlerini `AskOutcome`'a uyarla**

`run()` içinde üç yer:

1. **Slug dalı** (Task 1'deki blok):
```rust
                    Ok(AskOutcome::Reply(reply)) => crate::finalize_slug(&raw, &reply.text),
                    Ok(AskOutcome::Cancelled) | Err(_) => crate::slugify_topic(&raw),
```
(reset satırı yerinde kalır — iptal de CLI oturumu yakalamış olabilir.)

2. **Açılış drilli/tanışma:**
```rust
        Ok(AskOutcome::Reply(reply)) => {
            last_tokens = reply.context_tokens;
            page_reply(&mut tui, &reply.text, width)?;
            recorder.assistant(&reply.text);
            session.push_assistant(reply.text);
        }
        Ok(AskOutcome::Cancelled) => {
            backend.reset_session();
            page_notice(&mut tui, "açılış turu iptal edildi")?;
        }
        Err(e) => page_notice(&mut tui, &format!("açılış turu atlandı: {e}"))?,
```

3. **Ana döngü Submit:**
```rust
                            Ok(AskOutcome::Reply(reply)) => {
                                last_tokens = reply.context_tokens;
                                page_reply(&mut tui, &reply.text, width)?;
                                recorder.assistant(&reply.text);
                                session.push_assistant(reply.text);
                                crate::maybe_compact(backend, &mut session, project_root, last_tokens).await;
                            }
                            Ok(AskOutcome::Cancelled) => {
                                // User turn history'de kalır (bilinçli — spec B2); CLI oturumu
                                // yarım — resume edilmesin, sonraki çağrı tam transcript'le gitsin.
                                backend.reset_session();
                                page_notice(&mut tui, "yanıt iptal edildi — mesajın kaldı, istersen devam et")?;
                            }
                            Err(e) => page_notice(&mut tui, &format!("hata: {e}"))?,
```

- [ ] **Step 7: Tüm testler + commit**

```bash
cargo test --quiet 2>&1 | tail -3
git add src/backend.rs src/tui/status.rs src/tui/run.rs
git commit -m "tui: çift Ctrl-C iptal — AskOutcome + kill_on_drop, kilitli LLM çağrısından kaçış yolu"
```

---

### Task 3: B3 — sayfa başına güncel genişlik

**Files:**
- Modify: `src/tui/run.rs`

**Interfaces:**
- Produces: `fn current_width(tui: &Tui) -> u16` (run.rs-içi).

- [ ] **Step 1: Yardımcıyı ekle**

```rust
/// Anlık terminal genişliği — resize sonrası sarma doğru kalsın (spec B3).
/// Ölçüm başarısızsa 80'e düş (sarma bozulmaz, sadece dar olur).
fn current_width(tui: &Tui) -> u16 {
    tui.terminal.size().map(|s| s.width).unwrap_or(80)
}
```

- [ ] **Step 2: Kullanım noktalarını değiştir**

`run()` başındaki `let width = tui.terminal.size()?.width;` satırı SİLİNİR. Genişlik kullanan her nokta çağrı anında ölçer:

- `ask_topic(..., width)` çağrısı → `ask_topic` imzasından `width` parametresi kalkar, fonksiyon içinde `current_width(tui)` (render_welcome_identity çağrısından hemen önce).
- `page_reply(&mut tui, &reply.text, width)` (3 yer) → `let w = current_width(&tui); page_reply(&mut tui, &reply.text, w)?;`
- `welcome::render_welcome(&data, width)` → `current_width(&tui)`.

Borrow notu: `current_width(&tui)` immutable borrow — `page_reply(&mut tui, ...)` çağrısından ÖNCE ayrı satırda değişkene al (yukarıdaki kalıp), aynı ifadede iki borrow deneme.

- [ ] **Step 3: Testler + commit**

```bash
cargo test --quiet 2>&1 | tail -3
git add src/tui/run.rs
git commit -m "tui: genişlik her sayfalamada ölçülür — resize sonrası sarma düzgün"
```

---

### Task 4: B4 — event stream sonu guard'ı + elle doğrulama + kurulum

**Files:**
- Modify: `src/tui/run.rs` (`ask_topic`, `tui_confirm`, ana döngü)

- [ ] **Step 1: Stream `None` = çıkış**

`ask_topic` döngüsü:

```rust
    loop {
        draw(tui, editor, &Status::Idle, None, 0)?;
        match events.next().await {
            Some(Ok(Event::Key(k))) => match editor.handle_key(k) {
                Action::Submit(line) => return Ok(Some(line)),
                Action::Exit => return Ok(None),
                Action::None => {}
            },
            Some(Ok(_)) | Some(Err(_)) => {} // resize vb. — yoksay
            None => return Ok(None), // stream bitti — sıcak döngüye girme (spec B4)
        }
    }
```

`tui_confirm` döngüsü aynı kalıpla: `None => return Ok(false)`.

Ana döngü: `Some(Ok(ev)) = events.next()` select kolu `maybe_ev = events.next()` olur; gövdede:

```rust
            maybe_ev = events.next() => {
                let Some(Ok(ev)) = maybe_ev else {
                    if maybe_ev.is_none() { break; } // stream bitti = Eof (spec B4)
                    continue; // tek olay hatası — yoksay
                };
                let Event::Key(k) = ev else { continue };
                // ... mevcut match aynen
            }
```

- [ ] **Step 2: Tüm testler + plain regresyon**

```bash
cargo test --quiet 2>&1 | tail -3
NO_COLOR=1 echo "" | cargo run --quiet -- start rust 2>&1 | head -5   # plain yol değişmemiş olmalı
```

- [ ] **Step 3: Elle doğrulama (spec §5 — gerçek terminalde, sırayla)**

1. `cargo run -- start` → cümleyle konu anlat → slug üretilir → ilk Usta yanıtında slug bağlamı sızıntısı yok; `ps | grep claude`'da öksüz süreç yok.
2. Yanıt beklerken Ctrl-C → durum satırında "(iptal: tekrar Ctrl-C)"; ikinci Ctrl-C → "yanıt iptal edildi", girdi kutusu aktif; sonraki mesaj normal yanıt alıyor.
3. Slug beklerken çift Ctrl-C → yerel slug ile oturum kuruluyor.
4. Terminali daralt/genişlet → sonraki yanıt yeni genişliğe sarıyor.
5. Ctrl-C/D idle'da → temiz çıkış, shell bozuk değil.

- [ ] **Step 4: Kurulum + commit**

```bash
cargo install --path .
git add src/tui/run.rs
git commit -m "tui: event stream sonu guard'ı — None = çıkış, sıcak döngü yok; elle doğrulama geçti"
```

---

## Self-Review Notları

- **Spec kapsaması:** B1→Task 1, B2→Task 2, B3→Task 3, B4→Task 4, elle doğrulama→Task 4 Step 3. Boşluk yok.
- **Görev sırası bilinçli:** Task 1 reset satırı Task 2'nin match yeniden-yazımında yerinde kalır (Task 1 Step 4'teki NOT bunu implementasyoncuya söyler).
- **Tip tutarlılığı:** `AskOutcome` Task 2'de tanımlanır, üç çağrı yeri aynı görevde güncellenir — yarım durumda derlenmeyen commit yok.
- **Satır numaraları** `aaf99d8` sonrası main'e göre — kaymışsa fonksiyon adıyla bul (plan başındaki not).
