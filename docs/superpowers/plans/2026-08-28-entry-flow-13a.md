# Entry Flow 13a — Introduction Before Topic Lock — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Ön-koşul:** v0.26.2 üstüne. Spec: `docs/superpowers/specs/2026-08-28-entry-flow-rewrite-design.md` — önce oku, özellikle "İsimlendirme (bağlayıcı)" ve "13a Davranış" bölümleri. Bu plan YALNIZ 13a'yı kapsar; 13b (curriculum terfisi, plan üretimi) ve 13c bu plana GİRMEZ.

**Goal:** Bare `usta` ilk çalıştırmada topic kilitlenmeden ÖNCE tanışma konuşması yapar (üç kural, soru tavanı yok); model `TOPIC: <slug>` son satırıyla topic'i kilitler; konuşma gerçek oturumun başı olarak devam eder. Dönen kullanıcının boş-Enter önerisi de aynı konuşmalı mekanizmayı kullanır (profil + PROJECT.md görerek). `topic::start_suggest_system` + `parse_start_suggestion` + yes/no onay kapısı silinir. Çıkarılan rol closing'de approach dosyasına `role:` satırı olarak yazılır. Kalıcı ilk-çalıştırma işareti: `<global>/learner/.introduced`. v0.27.0.

**Architecture:** Saf mantık üç yerde: marker parse `src/topic.rs`, işaret dosyası `src/setup.rs`, prompt üreticileri `src/progress.rs`. Konuşma döngüsü + stitch yeni `src/tui/intro.rs` modülünde. `src/tui/run.rs` (585 satır / 600 bütçe, belgeli istisna — BÜYÜTME) yalnız ince bağlantı alır; identity-welcome baskısı `entry.rs`'te yardımcıya çıkarılır ki intro yolu tekrar kullanabilsin. Plain/pipe yolu, `usta start <topic>`, resume akışı, lock/katalog/transcript ve closing flush sözleşmesi DEĞİŞMEZ (spec H4).

**Tech Stack:** Rust, tokio (`select!` yok — intro kendi basit döngüsünü kurar), ratatui inline viewport, crossterm `EventStream`. Binary crate — test filtreli `cargo test <filtre>`.

## Global Constraints

- TÜM yeni kod adları, kullanıcıya dönük string'ler, kod yorumları, commit mesajları İNGİLİZCE (spec "İsimlendirme" bölümü birebir bağlayıcı).
- Her task: TDD — önce failing test, sonra minimal implementasyon; task sonunda `cargo test` TÜMÜ yeşil, sonra commit + push.
- `cargo clippy --all-targets` her task sonunda 0 uyarı; `cargo fmt` yalnız dokunulan dosyalara scoped; en sonda `cargo fmt --check` crate-genelinde temiz.
- `src/tui/run.rs` 600 satırı AŞAMAZ (`grep -c "" src/tui/run.rs` ≤ 600). Aşarsa mantık `intro.rs`'e taşınır, `run.rs`'e yalnız çağrı kalır.
- Prompt diet (roadmap #9): shell'in deterministik çözebildiği hiçbir şey prompt'a binmez — marker parse, işaret dosyası, tohumlama tamamı shell'de; model yalnız `TOPIC:` satırını üretir ve `role:` satırını yazar.
- Davranış regresyonu yasak: plain/pipe/CI yolu (`src/plain.rs` bu planda HİÇ değişmez), `usta start <topic>`, resume, lock-çakışma onayı, katalog upsert, transcript, closing flush (`closing_prompt` yalnız metin kazanır, dosya seti 6'da kalır).
- `role:` satırını 13a'da shell PARSE ETMEZ (YAGNI — 13b ekler); token'lar yine de `src/tokens.rs`'te tek-kaynak.

---

### Task 1: Topic marker — token + parse (`src/tokens.rs`, `src/topic.rs`)

**Files:**
- Modify: `src/tokens.rs` (dosya sonundaki "File / flow markers." bloğuna 1 sabit)
- Modify: `src/topic.rs` (yeni `parse_topic_marker` + testleri mevcut `mod tests`'e)

**Interfaces:**
- Consumes: `topic::slugify_topic` (mevcut), `tokens` sabit deseni.
- Produces: `pub const TOPIC_MARKER: &str = "TOPIC:";` (tokens.rs) · `pub(crate) fn parse_topic_marker(reply: &str) -> Option<(String, String)>` — dönen `(slug, display)`: slug `slugify_topic` garantili; display = marker satırı düşülmüş metin (trim'li). Yalnız SON boş-olmayan satır eşleşir.

- [ ] **Step 1: Failing testler** — `src/topic.rs` test modülüne ekle:

```rust
#[test]
fn parse_topic_marker_extracts_slug_and_display() {
    let reply =
        "Great — let's begin with ownership.\nFirst step: cargo new.\nTOPIC: rust-ownership";
    let (slug, display) = parse_topic_marker(reply).unwrap();
    assert_eq!(slug, "rust-ownership");
    assert!(display.contains("First step"));
    assert!(!display.contains("TOPIC:"));
}

#[test]
fn parse_topic_marker_normalizes_messy_slug() {
    let (slug, _) = parse_topic_marker("ok\nTOPIC: Rust Temelleri!").unwrap();
    assert_eq!(slug, "rust-temelleri");
}

#[test]
fn parse_topic_marker_only_matches_final_line() {
    // Marker mid-reply is NOT a lock — only the final line counts.
    assert!(parse_topic_marker("TOPIC: rust\nbut then more prose").is_none());
}

#[test]
fn parse_topic_marker_rejects_blank_or_missing() {
    assert!(parse_topic_marker("no marker here").is_none());
    // Blank slug must be None BEFORE slugify_topic's "general" fallback kicks in
    // (same guard parse_start_suggestion documents today).
    assert!(parse_topic_marker("text\nTOPIC:   ").is_none());
}

#[test]
fn parse_topic_marker_accepts_marker_only_reply() {
    let (slug, display) = parse_topic_marker("TOPIC: golang-web").unwrap();
    assert_eq!(slug, "golang-web");
    assert_eq!(display, "");
}
```

- [ ] **Step 2:** `cargo test parse_topic_marker` → derleme hatası (fonksiyon yok). Beklenen: FAIL/derlenmez.
- [ ] **Step 3: Minimal implementasyon.** `src/tokens.rs` "File / flow markers." bloğuna:

```rust
/// Pre-lock conversation topic lock: the model's FINAL reply line
/// `TOPIC: <slug>` locks the topic (SPEC §4.22).
pub const TOPIC_MARKER: &str = "TOPIC:";
```

`src/topic.rs`'e (`parse_start_suggestion`'ın yanına — Task 5'te o silinecek, bu kalacak):

```rust
/// Parse the pre-lock conversation's topic lock (SPEC §4.22): if the reply's
/// FINAL non-empty line is `TOPIC: <slug>`, return (slug, display text without
/// that line). Mid-reply markers do NOT lock — only the final line counts, so
/// prose that merely mentions the token can't hijack the flow. Blank slug →
/// None (checked BEFORE slugify_topic, which would otherwise fall back to
/// tokens::DEFAULT_TOPIC — same guard parse_start_suggestion used).
pub(crate) fn parse_topic_marker(reply: &str) -> Option<(String, String)> {
    let trimmed = reply.trim_end();
    let (head, last) = match trimmed.rsplit_once('\n') {
        Some((h, l)) => (h, l.trim()),
        None => ("", trimmed.trim()),
    };
    let rest = last.strip_prefix(tokens::TOPIC_MARKER)?;
    if rest.trim().is_empty() {
        return None;
    }
    // Hyphens already inside the value would be stripped by slugify_topic's
    // whitespace split — turn them into spaces first (finalize_slug's trick).
    let slug = slugify_topic(&rest.replace(['-', '_'], " "));
    Some((slug, head.trim().to_string()))
}
```

- [ ] **Step 4:** `cargo test parse_topic_marker` → 5 test PASS. Ardından `cargo test` (tümü) → yeşil.
- [ ] **Step 5:** `cargo fmt -- src/tokens.rs src/topic.rs` · `cargo clippy --all-targets` → 0 uyarı.
- [ ] **Step 6:** Commit + push: `feat: topic lock marker — TOPIC: token + final-line parse (13a)`

---

### Task 2: Kalıcı ilk-çalıştırma işareti (`src/setup.rs`)

**Files:**
- Modify: `src/setup.rs` (üç fonksiyon; `profile_is_generic`'in yakınına)
- Test: `src/setup_tests.rs` (mevcut `#[path]` test dosyası)

**Interfaces:**
- Consumes: `setup::profile_is_generic` (setup.rs:319), `index::entries`, `lifecycle::today()`, `defaults::global_defaults` (testte).
- Produces: `pub(crate) fn intro_marker_path(global: &Path) -> PathBuf` (= `global.join("learner/.introduced")`) · `pub(crate) fn mark_intro_done(global: &Path, how: &str)` (best-effort, hata sessiz — lock yazımı emsali) · `pub(crate) fn intro_needed(global: &Path, index_content: &str) -> bool` (kendi kendini tohumlar: kanıt varsa `seeded` yazar ve false döner).

- [ ] **Step 1: Failing testler** — `src/setup_tests.rs`'e ekle:

```rust
fn temp_global(name: &str) -> std::path::PathBuf {
    let dir = std::env::temp_dir().join(format!("usta_intro_{name}_{}", std::process::id()));
    let _ = std::fs::remove_dir_all(&dir);
    std::fs::create_dir_all(&dir).unwrap();
    dir
}

#[test]
fn intro_needed_true_on_fresh_global_and_does_not_self_seed() {
    let g = temp_global("fresh");
    assert!(intro_needed(&g, ""));
    // No evidence → no marker written; asking again still says true.
    assert!(!intro_marker_path(&g).exists());
    assert!(intro_needed(&g, ""));
    let _ = std::fs::remove_dir_all(&g);
}

#[test]
fn intro_needed_false_after_mark_done() {
    let g = temp_global("done");
    mark_intro_done(&g, "completed");
    assert!(!intro_needed(&g, ""));
    let content = std::fs::read_to_string(intro_marker_path(&g)).unwrap();
    assert!(content.contains("completed"));
    let _ = std::fs::remove_dir_all(&g);
}

#[test]
fn intro_needed_seeds_from_filled_profile() {
    let g = temp_global("profile");
    std::fs::write(g.join("USER.md"), "# Learner Profile — Ada\npersonal content").unwrap();
    assert!(!intro_needed(&g, ""));
    // Evidence found → the marker is written as "seeded" (grandfathering).
    let content = std::fs::read_to_string(intro_marker_path(&g)).unwrap();
    assert!(content.contains("seeded"));
    let _ = std::fs::remove_dir_all(&g);
}

#[test]
fn intro_needed_seeds_from_catalog_records() {
    let g = temp_global("catalog");
    let index = "## Records\n- rust | /tmp/proj | 2026-08-01\n";
    assert!(!intro_needed(&g, index));
    assert!(intro_marker_path(&g).exists());
    let _ = std::fs::remove_dir_all(&g);
}

#[test]
fn intro_needed_treats_empty_or_generic_profile_as_unknown_user() {
    let g = temp_global("generic");
    // Empty file: profile_is_generic("") is false (no template match) — the
    // helper must NOT read that as "filled profile" (spec H3 empty-string trap).
    std::fs::write(g.join("USER.md"), "").unwrap();
    assert!(intro_needed(&g, ""));
    // The embedded template itself is generic → still needed.
    let template = crate::defaults::global_defaults()
        .into_iter()
        .find(|(rel, _, _)| *rel == "USER.md")
        .map(|(_, c, _)| c)
        .unwrap();
    std::fs::write(g.join("USER.md"), template).unwrap();
    assert!(intro_needed(&g, ""));
    let _ = std::fs::remove_dir_all(&g);
}

#[test]
fn profile_reset_keeps_intro_marker() {
    let g = temp_global("resetprofile");
    mark_intro_done(&g, "completed");
    reset_profile_files(&g).unwrap();
    assert!(intro_marker_path(&g).exists());
    let _ = std::fs::remove_dir_all(&g);
}
```

Not: `setup_tests.rs` `use super::*` ile `setup.rs`'in private öğelerine erişir (`reset_profile_files` private — mevcut desen). `temp_global` adı mevcut yardımcılarla çakışırsa (dosyayı kontrol et) `intro_temp_global` kullan ve testlerde adı değiştir.

- [ ] **Step 2:** `cargo test intro_needed` → derlenmez. Beklenen: FAIL.
- [ ] **Step 3: Minimal implementasyon** — `src/setup.rs`, `profile_is_generic`'in hemen altına:

```rust
/// First-run marker: `<global>/learner/.introduced`. Its EXISTENCE is the only
/// thing the shell reads (deterministic); the content is diagnostics.
/// `profile_is_generic` can't carry first-run detection — one hand edit to
/// USER.md would skip the introduction forever, and a profile reset would
/// re-trigger it for a veteran (SPEC §4.22, blocker H3).
pub(crate) fn intro_marker_path(global: &Path) -> PathBuf {
    global.join("learner/.introduced")
}

/// Write the first-run marker (best-effort — a marker write failure must never
/// block the session; worst case the introduction re-runs next launch).
/// `how`: "completed" (introduction finished) or "seeded" (grandfathered).
pub(crate) fn mark_intro_done(global: &Path, how: &str) {
    let path = intro_marker_path(global);
    if let Some(parent) = path.parent() {
        let _ = std::fs::create_dir_all(parent);
    }
    let _ = std::fs::write(&path, format!("{} | {how}\n", crate::lifecycle::today()));
}

/// Does the first-run introduction still need to run? Self-seeding: when the
/// marker is absent but there is evidence of prior use (a filled profile, or
/// any catalog record), the marker is written as "seeded" and the introduction
/// is skipped — existing users are grandfathered on their first post-upgrade
/// launch. An empty/missing USER.md is NOT evidence (profile_is_generic("")
/// returns false because it matches nothing — guard it explicitly).
pub(crate) fn intro_needed(global: &Path, index_content: &str) -> bool {
    if intro_marker_path(global).exists() {
        return false;
    }
    let profile = std::fs::read_to_string(global.join("USER.md")).unwrap_or_default();
    let filled_profile = !profile.trim().is_empty() && !profile_is_generic(&profile);
    let has_records = !crate::index::entries(index_content).is_empty();
    if filled_profile || has_records {
        mark_intro_done(global, "seeded");
        return false;
    }
    true
}
```

Gerekirse `use` bloğuna ekleme yapma — `setup.rs` zaten `Path`/`PathBuf` kullanıyor; `crate::index` / `crate::lifecycle` tam yolla çağrılıyor.

- [ ] **Step 4:** `cargo test intro` → 6 yeni test PASS (`profile_reset_keeps_intro_marker` dahil — filtreye girmezse `cargo test profile_reset_keeps` ayrıca). `cargo test` (tümü) → yeşil.
- [ ] **Step 5:** `cargo fmt -- src/setup.rs src/setup_tests.rs` · `cargo clippy --all-targets` → 0 uyarı.
- [ ] **Step 6:** Commit + push: `feat: persisted first-run marker with evidence seeding (13a)`

---

### Task 3: Rol token'ları + prompt üreticileri + closing kuralları (`src/tokens.rs`, `src/progress.rs`)

**Files:**
- Modify: `src/tokens.rs` (rol sabitleri)
- Modify: `src/progress.rs` (`introduction_prompt`, `start_here_prompt`, `topic_marker_rule`, `material_block` çıkarımı; `closing_prompt` metin ekleri)
- Test: `src/progress_tests.rs`

**Interfaces:**
- Consumes: `tokens::TOPIC_MARKER` (Task 1), mevcut `onboarding_prompt` material bloğu.
- Produces: `pub const ROLE_PREFIX: &str = "role: ";` + `ROLE_GUIDED = "guided"` + `ROLE_PROJECT = "project"` + `ROLE_OBSERVATION = "observation"` (tokens.rs) · `pub fn introduction_prompt(project_known: bool, materials: Option<&str>) -> String` · `pub fn start_here_prompt() -> String` (progress.rs). `closing_prompt` approach kuralı `role:` satırını, curriculum kuralı observation istisnasını tanımlar.

- [ ] **Step 1: Failing testler** — `src/progress_tests.rs`'e ekle:

```rust
#[test]
fn introduction_prompt_carries_rules_marker_and_role() {
    let p = introduction_prompt(false, None);
    assert!(p.contains("[FIRST RUN — INTRODUCTION]"));
    assert!(p.contains("changes what you do next")); // rule (i)
    assert!(p.contains("accumulation, not interrogation")); // rule (ii)
    assert!(p.contains("honor it without argument")); // rule (iii)
    assert!(p.contains("short answers")); // no boredom inference from terse replies
    assert!(p.contains(crate::tokens::TOPIC_MARKER));
    assert!(p.contains("never ask as a menu"));
    assert!(p.contains("role: guided|project|observation"));
    assert!(p.contains("Hard Rule 6"));
    // The old 1-2 question cap must NOT ride in this prompt (spec A).
    assert!(!p.contains("1-2 questions"));
    // project-unknown flavor asks for the project naturally.
    assert!(p.contains("There is no mentor/PROJECT.md yet"));
    let known = introduction_prompt(true, None);
    assert!(known.contains("do NOT re-ask project basics"));
}

#[test]
fn introduction_prompt_embeds_material_digest() {
    let p = introduction_prompt(false, Some("DIGEST-X"));
    assert!(p.contains("[COURSE MATERIAL FOUND]"));
    assert!(p.contains("DIGEST-X"));
    assert!(!introduction_prompt(false, None).contains("[COURSE MATERIAL FOUND]"));
}

#[test]
fn start_here_prompt_calibrates_and_uses_marker() {
    let s = start_here_prompt();
    assert!(s.contains("[START SUGGESTION]"));
    assert!(s.contains("CALIBRATED"));
    assert!(s.contains("mentor/PROJECT.md"));
    assert!(s.contains("USER.md"));
    assert!(s.contains(crate::tokens::TOPIC_MARKER));
    assert!(s.contains("Hard Rule 6"));
}

#[test]
fn closing_prompt_defines_role_line_and_observation_map_exception() {
    let s = closing_prompt("rust", None, None, None, None, None, None);
    assert!(s.contains("role: guided|project|observation"));
    assert!(s.contains("role: observation"));
    assert!(s.contains("evidenced in sessions"));
}
```

(`progress_tests.rs` başındaki import düzenine bak — `use super::*;` varsa `crate::tokens` yolu çalışır.)

- [ ] **Step 2:** `cargo test introduction_prompt` → derlenmez. Beklenen: FAIL.
- [ ] **Step 3: Implementasyon.**

`src/tokens.rs` — "Bare section names" bloğunun yanına:

```rust
// Approach-file role field (SPEC §4.22): inferred, never asked as a menu.
// The shell does NOT parse it in 13a (13b adds the reader) — single-sourced
// here because two prompts embed the same strings.
pub const ROLE_PREFIX: &str = "role: ";
pub const ROLE_GUIDED: &str = "guided";
pub const ROLE_PROJECT: &str = "project";
pub const ROLE_OBSERVATION: &str = "observation";
```

`src/progress.rs` — önce `onboarding_prompt`'taki `material_block` inline `match`'ini private yardımcıya çıkar (davranış birebir; `onboarding_prompt` bu yardımcıyı çağırır):

```rust
/// Material-digest block shared by the new-topic onboarding turn and the
/// first-run introduction (SPEC §4.22) — behavior identical to the block
/// previously inlined in `onboarding_prompt`.
fn material_block(materials: Option<&str>) -> String {
    match materials {
        Some(d) => format!(
            "\n[COURSE MATERIAL FOUND]\nThe user has material under materials/ — \
             outline digests below. ASK whether to anchor this topic's curriculum \
             to this material (it may belong to another topic). If yes: build the \
             curriculum map FROM its chapters/sections — each map item carries a \
             source ref (`{src} <file> §<section>`); assign reading from it \
             (the USER reads — you don't summarize the material into the chat); \
             still add critical items the material lacks, from web research \
             (scope guarding). If no: proceed normally.\n---\n{d}\n---\n",
            src = tokens::SOURCE_DASH,
        ),
        None => String::new(),
    }
}
```

Sonra iki yeni prompt + paylaşılan marker kuralı:

```rust
/// Shared topic-lock contract for the pre-lock conversation prompts
/// (SPEC §4.22): the reply's FINAL line locks the topic; the shell parses
/// nothing else out of the conversation (prompt diet).
fn topic_marker_rule() -> String {
    format!(
        "When the user agrees to start — or asks you to just pick — end that \
         reply with a FINAL line exactly `{marker} <topic-slug>` (lowercase, \
         hyphenated, 1-3 words; nothing after that line; never emit it before \
         agreement). The shell locks the topic on that line and the session \
         simply continues — do not restart the introduction after it.",
        marker = tokens::TOPIC_MARKER,
    )
}

/// First-run conversation turn — sent BEFORE any topic is locked (SPEC §4.22).
/// Deliberately has NO question cap: three conversation rules govern length
/// instead (rule iii keeps it short when the user wants it short). MEET_BLOCK
/// stays as-is for the post-lock surfaces (plain path, `usta start`).
pub fn introduction_prompt(project_known: bool, materials: Option<&str>) -> String {
    let project_block = if project_known {
        "\nThe project files mentor/PROJECT.md and mentor/PROGRESS.md are in your \
         system prompt — anchor the conversation and your starting proposal to \
         that project; do NOT re-ask project basics.\n"
    } else {
        "\nThere is no mentor/PROJECT.md yet. Find out, naturally (not as a \
         form): what they're building or want to build, why, rough scale, \
         stack/tools and why. At session close you'll be asked for a `project` \
         file — the shell writes it.\n"
    };
    format!(
        "[FIRST RUN — INTRODUCTION]\n\
         This is the user's first session ever and no topic is chosen yet. Get \
         to know the learner, infer what they want from you, and end by \
         proposing where to start.\n\
         Three rules replace any question limit:\n\
         1. Ask ONLY questions whose answer changes what you do next. No \
         inventory quizzing (\"do you know X? and Y?\") — real knowledge \
         surfaces during work anyway.\n\
         2. As you go, reflect back in a sentence or two what you have \
         understood so far — the conversation must read as accumulation, not \
         interrogation.\n\
         3. Announce the exit ONCE, early and plainly: they can say \"let's \
         start\" at any time and you'll learn the rest as you work. When they \
         say it, honor it without argument.\n\
         Never infer boredom or disinterest from short answers — a yes/no \
         question earns a yes/no; judge by what they say, not how much.\n\
         Also infer — never ask as a menu — what they want from you: guided \
         learning (you drive the route) / their own project (learn while \
         building it) / observation (they know their craft; mainly review and \
         watching). If it won't emerge on its own, ask at most ONE jargon-free \
         question about what they'd like you to do. At session close this lands \
         in the approach file as a `{role_prefix}{guided}|{project}|{observation}` \
         line.\n\
         {project_block}{material_block}\
         When the direction is clear, propose ONE starting topic: why this \
         first, and one concrete first step small enough to start today. \
         {marker_rule}\n\
         At session close you'll be asked for the approach + FULL curriculum map \
         content and the user profile; the shell writes the files — never write \
         files yourself during the session (Hard Rule 6).",
        role_prefix = tokens::ROLE_PREFIX,
        guided = tokens::ROLE_GUIDED,
        project = tokens::ROLE_PROJECT,
        observation = tokens::ROLE_OBSERVATION,
        project_block = project_block,
        material_block = material_block(materials),
        marker_rule = topic_marker_rule(),
    )
}

/// Conversational start suggestion for a RETURNING user (empty Enter with a
/// filled mentor/PROJECT.md and no local topics) — replaces the one-shot
/// `start_suggest_system` + its yes/no confirm gate (SPEC §4.22). Runs on the
/// full brain system prompt, so the profile and SOUL language lock ride along
/// for free (the old bare one-shot needed a hand-written English note).
pub fn start_here_prompt() -> String {
    format!(
        "[START SUGGESTION]\n\
         You already know this user (USER.md in your system prompt) and their \
         project (mentor/PROJECT.md). No topic is chosen yet — they asked you \
         to propose where to start. Propose ONE starting topic CALIBRATED to \
         the background and level in their profile: why this first, and one \
         concrete first step small enough to start today. Keep it short, no \
         greeting. If something essential is genuinely unknown, ask at most one \
         question first. {marker_rule}\n\
         At session close you'll be asked for the approach + FULL curriculum \
         map content; the shell writes the files — never write files yourself \
         during the session (Hard Rule 6).",
        marker_rule = topic_marker_rule(),
    )
}
```

`closing_prompt` içinde İKİ metin eki (format string'i düzenle, satır sarmalarını koru):

1. Approach kuralı — `- \`approach\` is generated only on the first session ... (practice / output / feedback).` cümlesinden hemen SONRA, `For goal-directed learning`'den önce şu cümle eklenir:

```
Its FIRST line under the heading is `role: guided|project|observation` — the
mentoring role inferred from how the user works with you (guided = you drive
the route; project = they build their own project while learning; observation
= an expert who mainly wants review and watching). Keep it stable across
sessions; change it only on clear behavioral evidence, and say so in one
sentence when you do.
```

Bu blok format string'e girerken `role: guided|project|observation` kısmını token'larla yaz: `{role_prefix}{r_guided}|{r_project}|{r_observation}` ve format argümanlarına `role_prefix = tokens::ROLE_PREFIX, r_guided = tokens::ROLE_GUIDED, r_project = tokens::ROLE_PROJECT, r_observation = tokens::ROLE_OBSERVATION` ekle.

2. Curriculum kuralı — `draw on web research if needed);` ifadesinden hemen SONRA eklenir:

```
EXCEPTION: if the approach records `role: observation`, do NOT extract a
speculative full map — list only items actually evidenced in sessions (gaps
observed, topics discussed);
```

(`role: observation` burada da `{role_prefix}{r_observation}` ile yazılır.)

- [ ] **Step 4:** `cargo test introduction_prompt start_here closing_prompt` filtreleri ayrı ayrı → yeni 4 test PASS; `cargo test` (tümü) → yeşil (özellikle mevcut `onboarding_prompt` testleri — `material_block` çıkarımı davranış değiştirmemeli).
- [ ] **Step 5:** `cargo fmt -- src/tokens.rs src/progress.rs src/progress_tests.rs` · `cargo clippy --all-targets` → 0 uyarı.
- [ ] **Step 6:** Commit + push: `feat: introduction + start-here prompts, role field closing rules (13a)`

---

### Task 4: `src/tui/intro.rs` — saf çekirdek (turlar, stitch, sistem yükleme)

**Files:**
- Create: `src/tui/intro.rs` (testler in-module)
- Modify: `src/tui/mod.rs` (modül kaydı — mevcut kayıtların alfabetik/mevcut düzenine uyarak `pub(crate) mod intro;`)

**Interfaces:**
- Consumes: `anthropic::Message` (`user`, `assistant_raw`, `role` alanı), `session::Session` (`push_user`, `push_assistant`, `history`), `transcript::Recorder` (`user`, `assistant` — çağrı başına append eder, parent dizini kendi açar), `brain::load_system_prompt`, `tokens::DEFAULT_TOPIC`.
- Produces (Task 5 bunlara yaslanır):
  - `pub(crate) struct IntroTurn { pub user: bool, pub text: String }`
  - `pub(crate) enum IntroOutcome { Topic { slug: String, turns: Vec<IntroTurn> }, Fallback, Quit }`
  - `pub(crate) fn messages(turns: &[IntroTurn]) -> Vec<Message>`
  - `pub(crate) fn stitch(turns: &[IntroTurn], session: &mut Session, recorder: &Recorder)`
  - `pub(crate) fn intro_system(global: &Path, project_root: &Path, today: &str) -> String`

- [ ] **Step 1: Failing testler** — `src/tui/intro.rs`'i test modülüyle birlikte oluştur (implementasyon gövdeleri henüz `todo!()` DEĞİL — dosyayı komple Step 3'te yazacaksın; TDD burada "önce testi yaz, derlenmediğini gör" olarak işler):

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::session::Session;
    use crate::transcript::Recorder;

    fn sample_turns() -> Vec<IntroTurn> {
        vec![
            IntroTurn { user: true, text: "[FIRST RUN — INTRODUCTION] ...".into() },
            IntroTurn { user: false, text: "Hi — what brings you here?".into() },
            IntroTurn { user: true, text: "I want to learn Rust".into() },
        ]
    }

    #[test]
    fn messages_maps_roles_in_order() {
        let msgs = messages(&sample_turns());
        assert_eq!(msgs.len(), 3);
        assert_eq!(msgs[0].role, "user");
        assert_eq!(msgs[1].role, "assistant");
        assert_eq!(msgs[2].role, "user");
    }

    #[test]
    fn stitch_replays_into_session_and_transcript() {
        let mut session = Session::new("rust", "SYSTEM");
        let dir = std::env::temp_dir().join(format!("usta_intro_stitch_{}", std::process::id()));
        let _ = std::fs::remove_dir_all(&dir);
        std::fs::create_dir_all(&dir).unwrap();
        let recorder = Recorder::new(dir.join("rec.jsonl"));
        stitch(&sample_turns(), &mut session, &recorder);
        assert_eq!(session.history().len(), 3);
        assert_eq!(session.history()[0].role, "user");
        assert_eq!(session.history()[1].role, "assistant");
        let raw = std::fs::read_to_string(dir.join("rec.jsonl")).unwrap();
        assert_eq!(raw.lines().count(), 3);
        let _ = std::fs::remove_dir_all(&dir);
    }

    #[test]
    fn intro_system_loads_brain_with_default_topic() {
        let dir = std::env::temp_dir().join(format!("usta_intro_sys_{}", std::process::id()));
        let _ = std::fs::remove_dir_all(&dir);
        let (global, project) = (dir.join("g"), dir.join("p"));
        std::fs::create_dir_all(&global).unwrap();
        std::fs::create_dir_all(&project).unwrap();
        std::fs::write(global.join("SOUL.md"), "SOUL-CONTENT").unwrap();
        let sys = intro_system(&global, &project, "2026-08-28");
        assert!(sys.contains("SOUL-CONTENT"));
        assert!(sys.starts_with("===== TODAY =====\n2026-08-28"));
        let _ = std::fs::remove_dir_all(&dir);
    }
}
```

- [ ] **Step 2:** `cargo test intro_` → derlenmez (tipler yok). Beklenen: FAIL.
- [ ] **Step 3: Implementasyon** — dosyanın üst yarısı:

```rust
//! Pre-lock conversation core (SPEC §4.22): the first-run introduction and the
//! conversational start suggestion both run BEFORE a topic exists. On the
//! model's `TOPIC:` line the caller locks the slug, and `stitch` replays the
//! turns into the real session + transcript — the introduction IS the head of
//! the session, not a separate phase (that is what closes blocker H2: by any
//! flush, the topic exists, so the ordinary closing contract owns the output).

use std::path::Path;

use crate::anthropic::Message;
use crate::session::Session;
use crate::transcript::Recorder;

/// One conversation turn — plain text, so it can be replayed into both the
/// Session history and the transcript without unwrapping serde values.
pub(crate) struct IntroTurn {
    pub user: bool,
    pub text: String,
}

/// How the pre-lock conversation ended.
pub(crate) enum IntroOutcome {
    /// The model emitted the topic marker — lock `slug`, stitch `turns`.
    Topic { slug: String, turns: Vec<IntroTurn> },
    /// The FIRST model call failed or was cancelled — fall back to manual
    /// topic entry (the old "suggestion failed — type a topic" resilience).
    Fallback,
    /// The user quit — no session, no lock, no files (spec H2: quit before
    /// lock writes nothing, mirroring today's quit-at-topic-prompt).
    Quit,
}

/// Turns → backend messages. Assistant text is wrapped exactly like
/// `Session::push_assistant` wraps it (`Value::String`).
pub(crate) fn messages(turns: &[IntroTurn]) -> Vec<Message> {
    turns
        .iter()
        .map(|t| {
            if t.user {
                Message::user(t.text.as_str())
            } else {
                Message::assistant_raw(serde_json::Value::String(t.text.clone()))
            }
        })
        .collect()
}

/// Replay the pre-lock conversation into the real session + transcript.
/// Call AFTER `build_session`; the caller must also `backend.reset_session()`
/// so the refreshed system prompt (real topic) rides the next call — the CLI
/// backend's `--resume` would otherwise keep the pre-lock system.
pub(crate) fn stitch(turns: &[IntroTurn], session: &mut Session, recorder: &Recorder) {
    for t in turns {
        if t.user {
            session.push_user(&t.text);
            recorder.user(&t.text);
        } else {
            session.push_assistant(t.text.clone());
            recorder.assistant(&t.text);
        }
    }
}

/// System prompt for the pre-lock conversation: the FULL brain, loaded with
/// the placeholder topic — no topic-scoped files exist yet. (Accepted edge,
/// SPEC §4.22: a user who really has a topic named like the placeholder leaks
/// that progress into the intro's context; harmless, nothing is written.)
pub(crate) fn intro_system(global: &Path, project_root: &Path, today: &str) -> String {
    crate::brain::load_system_prompt(
        global,
        Some(project_root),
        crate::tokens::DEFAULT_TOPIC,
        today,
    )
}
```

`src/tui/mod.rs`'e modül kaydı.

- [ ] **Step 4:** `cargo test intro_` → 3 test PASS. `cargo test` (tümü) → yeşil.
- [ ] **Step 5:** `cargo fmt -- src/tui/intro.rs src/tui/mod.rs` · `cargo clippy --all-targets` → 0 uyarı.
- [ ] **Step 6:** Commit + push: `feat: intro module core — turns, stitch, pre-lock system prompt (13a)`

---

### Task 5: Konuşma döngüsü + run.rs kablolama + eski önerinin silinmesi

Tek task, çünkü: `run_intro` ancak `run.rs`'ten çağrılınca dead-code olmaktan çıkar ve `start_suggest_system`'in tek çağıranı bu kablolamayla kalkar — clippy'nin her adımda 0 uyarı kuralı üçünü aynı commit'e bağlar.

**Files:**
- Modify: `src/tui/intro.rs` (`run_intro` + `read_user_line`; pin testi)
- Modify: `src/tui/entry.rs` (`print_identity_welcome` çıkarımı; `ask_topic` onu çağırır)
- Modify: `src/tui/run.rs` (intro dalı; Suggest kolunun değişimi; stitch + opening kapısı)
- Modify: `src/topic.rs` (`start_suggest_system`, `parse_start_suggestion` + DÖRT testinin silinmesi: `start_suggest_system_defines_konu_contract`, `parse_start_suggestion_splits_slug_and_text`, `parse_start_suggestion_normalizes_messy_slug`, `parse_start_suggestion_tolerates_missing_text_rejects_missing_konu`)

**Interfaces:**
- Consumes: Task 1-4'ün tamamı; `tui::ask::ask_live(tui, editor, events, backend, system: &str, history: &[Message], tokens: Option<u64>) -> Result<AskOutcome>`; `tui::page::{draw, page_reply, page_notice, page_error, page_user_echo, current_width, handle_resize}`; `help::{is_help_command, help_text}`; `visual::parse_show_command`; `slash::parse_watch_command`.
- Produces:
  - `pub(crate) async fn run_intro(tui: &mut Tui, editor: &mut InputBox, events: &mut EventStream, backend: &mut Backend, system: &str, opening: &str) -> Result<IntroOutcome>` (intro.rs)
  - `pub(crate) fn print_identity_welcome(tui: &mut Tui, profile: Option<&str>, model: &str, dir: &str, local: &[String], other: &[String], project_known: bool, week_sessions: u32, streak: u32, show_prompt: bool) -> Result<()>` (entry.rs)

- [ ] **Step 1: Failing pin testi** — `src/tui/intro.rs` test modülüne (polite.rs H3 emsali — kablolama silinirse test kırılır):

```rust
#[test]
fn run_wires_intro_flow() {
    let src = include_str!("run.rs");
    for needle in [
        "intro_needed",
        "run_intro",
        "stitch(",
        "mark_intro_done",
        "introduction_prompt",
        "start_here_prompt",
        "reset_session()",
    ] {
        assert!(src.contains(needle), "run.rs lost its intro wiring: {needle}");
    }
    // The one-shot suggestion must be GONE from run.rs.
    assert!(!src.contains("start_suggest_system"));
    assert!(!src.contains("parse_start_suggestion"));
}
```

- [ ] **Step 2:** `cargo test run_wires_intro_flow` → FAIL (iğneler henüz yok).
- [ ] **Step 3: `run_intro` + `read_user_line`** — `src/tui/intro.rs`'e ekle (import bloğuna: `anyhow::Result`, `crossterm::event::{Event, EventStream}`, `futures_util::StreamExt`, `crate::backend::Backend`, `crate::tui::editor::{Action, InputBox}`, `crate::tui::status::Status`, `crate::tui::term::Tui`):

```rust
/// Wait for one submitted user line at the intro prompt. `None` = quit
/// (Esc/Ctrl-C via Action::Exit, or the event stream ended). Empty Enter is
/// swallowed — there is no resume sentinel before a topic exists. Modeled on
/// `entry::ask_topic`'s event loop.
async fn read_user_line(
    tui: &mut Tui,
    editor: &mut InputBox,
    events: &mut EventStream,
) -> Result<Option<String>> {
    loop {
        crate::tui::page::draw(tui, editor, &Status::Idle, None, 0, None)?;
        match events.next().await {
            Some(Ok(Event::Key(k))) => match editor.handle_key(k) {
                Action::Submit(line) => {
                    let raw = line.trim().to_string();
                    if raw.is_empty() {
                        continue;
                    }
                    return Ok(Some(raw));
                }
                Action::Exit => return Ok(None),
                Action::None => {}
            },
            Some(Ok(Event::Paste(s))) => editor.insert_str(&s),
            Some(Ok(Event::Resize(_, _))) => crate::tui::page::handle_resize(tui)?,
            Some(Ok(_)) | Some(Err(_)) => {}
            None => return Ok(None), // stream ended — don't spin (spec B4)
        }
    }
}

/// The pre-lock conversation loop (SPEC §4.22). `opening` is the hidden first
/// user turn (`introduction_prompt` or `start_here_prompt`); the model speaks
/// first. Every model reply is checked for the final-line `TOPIC:` marker —
/// on a hit the RAW reply stays in the turns (role alternation is preserved
/// even for a marker-only reply) while only the stripped text is displayed.
/// A failed/cancelled FIRST call returns `Fallback` (manual topic entry);
/// mid-conversation failures wait for the user instead of aborting.
pub(crate) async fn run_intro(
    tui: &mut Tui,
    editor: &mut InputBox,
    events: &mut EventStream,
    backend: &mut Backend,
    system: &str,
    opening: &str,
) -> Result<IntroOutcome> {
    let mut turns = vec![IntroTurn { user: true, text: opening.to_string() }];
    loop {
        match crate::tui::ask::ask_live(tui, editor, events, backend, system, &messages(&turns), None)
            .await
        {
            Ok(crate::tui::ask::AskOutcome::Reply(reply)) => {
                match crate::topic::parse_topic_marker(&reply.text) {
                    Some((slug, display)) => {
                        if !display.is_empty() {
                            let w = crate::tui::page::current_width(tui);
                            crate::tui::page::page_reply(tui, &display, w)?;
                        }
                        turns.push(IntroTurn { user: false, text: reply.text });
                        return Ok(IntroOutcome::Topic { slug, turns });
                    }
                    None => {
                        let w = crate::tui::page::current_width(tui);
                        crate::tui::page::page_reply(tui, &reply.text, w)?;
                        turns.push(IntroTurn { user: false, text: reply.text });
                    }
                }
            }
            Ok(crate::tui::ask::AskOutcome::Cancelled) | Err(_) if turns.len() == 1 => {
                backend.reset_session();
                return Ok(IntroOutcome::Fallback);
            }
            Ok(crate::tui::ask::AskOutcome::Cancelled) => {
                crate::tui::page::page_notice(tui, "cancelled — keep typing, or /quit")?;
            }
            Err(e) => crate::tui::page::page_error(tui, &format!("error: {e}"))?,
        }
        // User turn.
        loop {
            let Some(raw) = read_user_line(tui, editor, events).await? else {
                return Ok(IntroOutcome::Quit);
            };
            if crate::help::is_help_command(&raw) {
                crate::tui::page::page_notice(tui, crate::help::help_text())?;
                continue;
            }
            if raw.eq_ignore_ascii_case("/quit") {
                return Ok(IntroOutcome::Quit);
            }
            if crate::visual::parse_show_command(&raw).is_some()
                || crate::slash::parse_watch_command(&raw).is_some()
            {
                crate::tui::page::page_notice(
                    tui,
                    "that command works inside a session — we're still getting acquainted",
                )?;
                continue;
            }
            crate::tui::page::page_user_echo(tui, &raw)?;
            turns.push(IntroTurn { user: true, text: raw });
            break;
        }
    }
}
```

Not: `Ok(Cancelled) | Err(_) if turns.len() == 1` match-guard'ı iki pattern'i tek kolda toplar; clippy/rustc kabul eder (guard her iki pattern'e uygulanır) — derleyici itiraz ederse iki ayrı kola aç, davranış aynı kalsın.

- [ ] **Step 4: `print_identity_welcome` çıkarımı** — `src/tui/entry.rs`: `ask_topic` içindeki `if show_welcome { ... }` bloğunun gövdesini (identity welcome render + prompt satırı) şu yardımcıya taşı ve `ask_topic` onu `show_prompt: true` ile çağırsın:

```rust
/// Print the identity welcome (logo box + entry hint). Shared by `ask_topic`
/// (with the prompt line) and the first-run introduction path in `run.rs`
/// (without it — the model opens the conversation instead of a prompt).
#[allow(clippy::too_many_arguments)]
pub(crate) fn print_identity_welcome(
    tui: &mut Tui,
    profile: Option<&str>,
    model: &str,
    dir: &str,
    local: &[String],
    other: &[String],
    project_known: bool,
    week_sessions: u32,
    streak: u32,
    show_prompt: bool,
) -> Result<()> {
    let name = profile.and_then(welcome_data::extract_name);
    let width = crate::tui::page::current_width(tui);
    crate::tui::page::page(
        tui,
        welcome::render_welcome_identity(
            name.as_deref(),
            model,
            dir,
            local,
            other,
            project_known,
            width,
            week_sessions,
            streak,
        ),
    )?;
    if show_prompt {
        let prompt_line = if project_known && local.is_empty() {
            "What do you want to learn? (Enter = Usta suggests from PROJECT.md; or type a topic)"
        } else {
            "What do you want to learn? (a word, or describe it in a sentence)"
        };
        crate::tui::page::page_notice(tui, prompt_line)?;
    }
    Ok(())
}
```

(`ask_topic` içindeki eski blok `if show_welcome { print_identity_welcome(tui, profile, model, dir, local, other, project_known, week_sessions, streak, true)?; }` olur. Davranış birebir — mevcut welcome testleri değişmez.)

- [ ] **Step 5: `run.rs` kablolaması.** `src/tui/run.rs`'te dört değişiklik:

**(a)** `let topic = match topic_arg {` satırından HEMEN ÖNCE taşıyıcıyı tanımla:

```rust
    // 13a: turns of a pre-lock conversation (first-run introduction or
    // conversational start suggestion) — stitched into the session after
    // build_session; when set, the opening/onboarding turn is NOT injected.
    let mut intro_turns: Option<Vec<crate::tui::intro::IntroTurn>> = None;
```

**(b)** `None => { ... }` kolunda, `let mut welcome_shown = false;` satırından hemen sonra, `loop {`'tan önce ilk-çalıştırma dalı:

```rust
            // First run ever (no marker, no evidence): the introduction
            // replaces the topic prompt — the topic is locked by the model's
            // TOPIC: line (SPEC §4.22). Fallback keeps the manual entry loop.
            let mut preset_topic: Option<String> = None;
            if crate::setup::intro_needed(global, &index_content) {
                crate::tui::entry::print_identity_welcome(
                    &mut tui,
                    read(global.join("USER.md")).as_deref(),
                    &backend.label(),
                    &crate::tui::paint::short_dir(project_root),
                    &local,
                    &other,
                    project_known,
                    week_sessions,
                    streak,
                    false,
                )?;
                welcome_shown = true;
                for note in crate::materials::convert_pdfs(project_root) {
                    crate::tui::page::page_notice(&mut tui, &note)?;
                }
                let digest =
                    crate::materials::combined_digests(&crate::materials::scan(project_root));
                let system = crate::tui::intro::intro_system(global, project_root, today);
                match crate::tui::intro::run_intro(
                    &mut tui,
                    &mut editor,
                    &mut events,
                    backend,
                    &system,
                    &progress::introduction_prompt(project_known, digest.as_deref()),
                )
                .await?
                {
                    crate::tui::intro::IntroOutcome::Topic { slug, turns } => {
                        crate::setup::mark_intro_done(global, "completed");
                        crate::tui::page::page_notice(&mut tui, &format!("topic: {slug}"))?;
                        intro_turns = Some(turns);
                        preset_topic = Some(slug);
                    }
                    crate::tui::intro::IntroOutcome::Fallback => {
                        crate::tui::page::page_error(
                            &mut tui,
                            "introduction failed — type a topic",
                        )?;
                    }
                    crate::tui::intro::IntroOutcome::Quit => return Ok(None),
                }
            }
```

ve mevcut `loop {`'u şuna sar: `match preset_topic { Some(t) => t, None => loop { ... } }` (loop gövdesi aynen kalır; yalnız girinti değişir — `cargo fmt` halleder).

**(c)** Loop içindeki `Some(crate::topic::TopicChoice::Suggest) => { ... }` kolunun TÜM gövdesi (one-shot `ask_live` + `parse_start_suggestion` + `tui_confirm` bloğu, ~48 satır) şununla değişir:

```rust
                    Some(crate::topic::TopicChoice::Suggest) => {
                        // Conversational start suggestion (SPEC §4.22): runs on
                        // the FULL brain — profile + PROJECT.md ride along, and
                        // agreement happens in conversation instead of a yes/no
                        // gate ("no, something easier" now works).
                        let system = crate::tui::intro::intro_system(global, project_root, today);
                        match crate::tui::intro::run_intro(
                            &mut tui,
                            &mut editor,
                            &mut events,
                            backend,
                            &system,
                            &progress::start_here_prompt(),
                        )
                        .await?
                        {
                            crate::tui::intro::IntroOutcome::Topic { slug, turns } => {
                                crate::tui::page::page_notice(
                                    &mut tui,
                                    &format!("topic: {slug}"),
                                )?;
                                intro_turns = Some(turns);
                                break slug;
                            }
                            crate::tui::intro::IntroOutcome::Fallback => {
                                crate::tui::page::page_error(
                                    &mut tui,
                                    "suggestion failed — type a topic",
                                )?;
                            }
                            crate::tui::intro::IntroOutcome::Quit => return Ok(None),
                        }
                    }
```

**(d)** `build_session` çağrısından sonra, watcher-drain bloğundan ÖNCE stitch; ve opening bloğu `Option` kapısına alınır. Mevcut:

```rust
    let opening = if has_progress { ... } else { ... };
    session.push_user(&opening);
    recorder.user(&opening);
    match crate::tui::ask::ask_live( ... ) { ... }
```

Yeni:

```rust
    // 13a: the pre-lock conversation becomes the head of the real session —
    // replayed into history + transcript. The system prompt was rebuilt with
    // the real topic by build_session, and the CLI backend's --resume would
    // keep the old one: reset so the next call resends the stitched history
    // under the fresh system (maybe_compact precedent).
    if let Some(turns) = &intro_turns {
        crate::tui::intro::stitch(turns, &mut session, &recorder);
        backend.reset_session();
    }

    let opening: Option<String> = if intro_turns.is_some() {
        // The conversation is already flowing — the last stitched turn is the
        // model's proposal; the user speaks next. No opening injection.
        None
    } else if has_progress {
        Some(/* mevcut opening_prompt bloğu aynen */)
    } else {
        /* mevcut convert_pdfs + scan + onboarding_prompt bloğu aynen */
        Some(progress::onboarding_prompt(...))
    };
    if let Some(opening) = opening {
        session.push_user(&opening);
        recorder.user(&opening);
        match crate::tui::ask::ask_live( /* mevcut blok aynen, +1 girinti */ ) { ... }
    }
```

DİKKAT: intro yolunda `convert_pdfs`/`scan` zaten (b)'de koştu — `else` dalındaki materials bloğu yalnız intro'suz yeni-topic yolunda koşmaya devam eder (davranış bugünle birebir).

- [ ] **Step 6: Eski önerinin silinmesi.** `src/topic.rs`'ten `start_suggest_system` ve `parse_start_suggestion` fonksiyonlarını + başlıklarında sayılan DÖRT testi sil. `grep -rn "start_suggest\|parse_start_suggestion" src/` → 0 sonuç (bu adımın doğrulaması). `TopicChoice::Suggest` varyantı, `interpret_topic_input`, `new_topic_confirm_msg` ve testleri KALIR.
- [ ] **Step 7:** `cargo test` (tümü) → yeşil; `cargo test run_wires_intro_flow` → PASS. Satır bütçesi: `grep -c "" src/tui/run.rs` → ≤ 600 (beklenen ~595 civarı; aşarsa (b) dalının gövdesini `intro.rs`'te `run_first_run(...)` yardımcısına taşı, `run.rs`'te tek çağrı bırak).
- [ ] **Step 8:** `cargo fmt -- src/tui/intro.rs src/tui/entry.rs src/tui/run.rs src/topic.rs` · `cargo clippy --all-targets` → 0 uyarı (silinen fonksiyonlar başka yerde kullanılmadığından dead-code çıkmamalı; `print_identity_welcome` 10 argüman → `#[allow(clippy::too_many_arguments)]` mevcut).
- [ ] **Step 9: Elle doğrulama** (mümkünse; değilse task raporuna "yapılmadı" yaz — sonraki oturumda Anil koşar):
  - `HOME=$(mktemp -d) USTA_BACKEND=cli cargo run` → identity welcome + tanışma açılır; birkaç tur sonra "let's start" → `topic: <slug>` notisi → oturum sürer; `/quit` sonrası `.usta/learner/progress/<slug>.md` + approach'ta `role:` satırı.
  - Aynı temiz HOME'da `cargo run -- start rust` → tanışma YOK (topic-arg yolu değişmedi).
  - Dolu profil + `mentor/PROJECT.md` + boş Enter → konuşmalı öneri; "no, something easier" yönlendirmesi çalışır.
  - `echo "" | NO_COLOR=1 cargo run` → plain yol, `general`e düşer (regresyon yok).
- [ ] **Step 10:** Commit + push: `feat: introduction before topic lock — conversational entry, one-shot suggestion removed (13a)`

---

### Task 6: Dokümantasyon + sürüm (v0.27.0)

**Files:**
- Modify: `SPEC.md` (§4.22 yeni bölüm + §11'e karar maddesi)
- Modify: `README.md` (102 ve 108. satırlardaki öneri-akışı cümleleri)
- Modify: `docs/ROADMAP.md` (madde 13 Status hücresi)
- Modify: `Cargo.toml` (+`Cargo.lock`, `cargo check` ile)

**Interfaces:** — (yalnız dokümantasyon + sürüm; kod değişikliği yok)

- [ ] **Step 1:** `SPEC.md`'ye §4.21'den sonra ekle:

```markdown
## 4.22 Entry Flow — Introduction Before Topic Lock (v0.27)

- Bare `usta` on a true first run (no `<global>/learner/.introduced` marker, no
  evidence of prior use) opens a pre-lock introduction conversation instead of
  the topic prompt: no question cap — three rules govern it (every question must
  change what Usta does next; Usta reflects back what it understood as it goes;
  the exit is announced once and honored without argument). The model locks the
  topic with a final-line `TOPIC: <slug>` marker; the conversation is stitched
  into the real session (history + transcript) and no opening/onboarding turn is
  injected. Quit before lock writes nothing.
- The marker file is self-seeding: a filled profile or any catalog record
  grandfathers existing users (`seeded`); completion writes `completed`. Profile
  reset keeps the marker; factory reset removes it (introduction runs again).
- Empty Enter with a filled `mentor/PROJECT.md` and no local topics is now a
  conversational suggestion on the full brain (profile + project ride along),
  replacing the one-shot `start_suggest_system` and its yes/no confirm gate —
  agreement happens in conversation.
- The inferred mentoring role is recorded by the closing flush as the approach
  file's first line: `role: guided|project|observation` (tokens single-sourced;
  the shell does not parse it in 13a). With `role: observation` the first-close
  curriculum lists only session-evidenced items — no speculative full map.
- The plain/pipe path, `usta start <topic>`, resume, lock/catalog/transcript and
  the six-file closing flush are unchanged. Design:
  `docs/superpowers/specs/2026-08-28-entry-flow-rewrite-design.md`.
```

- [ ] **Step 2:** `SPEC.md` §11'in sonuna karar maddesi:

```markdown
- **Entry flow 13a (v0.27.0):** the introduction moved BEFORE topic lock as a
  re-ordering of the existing MEET_BLOCK conversation, not a new gated phase —
  its 1-2 question cap was deliberately dropped for the pre-lock conversation
  (three conversation rules replace the number; a cap may return later from real
  transcripts). Role is inferred, never asked as a menu, and lives in the topic's
  approach file (the `## Goal` precedent — not user-scoped, no role logic in the
  shell for 13a). First-run detection is a persisted marker
  (`learner/.introduced`) with evidence seeding — `profile_is_generic` cannot
  carry it. Blocker H2 closed structurally: the introduction is the head of the
  real session, so by any flush the topic exists and the ordinary closing
  contract owns the output; quitting before lock writes nothing (YAGNI: no
  profile-only flush).
```

- [ ] **Step 3:** `README.md` 102. satırdaki **Proactive start** paragrafını ve 108. satırdaki bare-`usta` cümlesini güncelle: "Confirm and go" / tek-atımlık öneri anlatımı yerine — first run opens a short introduction conversation before any topic is chosen (say "let's start" any time); with a filled `PROJECT.md`, pressing Enter on the empty topic prompt starts a conversational suggestion that already knows your profile and plan — steer it in conversation instead of a yes/no confirm. (İngilizce, README dili; iki paragrafın kalan içeriği korunur.)
- [ ] **Step 4:** `docs/ROADMAP.md` madde 13 Status hücresi: `pending (designed 2026-08-28, reviewed by a Fable adversarial pass)` → `13a ✅ (2026-08-28, v0.27.0) · 13b/13c pending`. Satırın Summary metnine dokunma (spec zemini olarak kalıyor).
- [ ] **Step 5:** `Cargo.toml` `version = "0.26.2"` → `"0.27.0"`; `cargo check` (Cargo.lock güncellenir).
- [ ] **Step 6: Final kapılar:** `cargo test` (tümü yeşil) · `cargo clippy --all-targets` (0 uyarı) · `cargo fmt --check` (crate-genelinde temiz) · `grep -c "" src/tui/run.rs` ≤ 600.
- [ ] **Step 7:** Commit + push: `docs: SPEC §4.22 + README + roadmap status; bump to v0.27.0 (13a)`

---

## Self-review notları (plan yazımında yapıldı)

- Spec kapsama: A (üç kural, tavan yok) → Task 3+5 · B (çıkarımsal rol, approach alanı) → Task 3 · öneri-öğrenciyi-görür → Task 5 (full-brain `intro_system`) · `start_suggest_system`+onay kapısı ölümü → Task 5 Step 6 · H2 → mimari (stitch, Task 4/5) · H3 → Task 2 · H4 → "plain.rs değişmez" global kısıtı · H1 → 13a'da köşe boyanmıyor (rol yalnız satır; slug rezervasyonu yok). C-G bu planda YOK (13b/13c — bilinçli).
- Tip tutarlılığı: `IntroOutcome::Topic { slug, turns }` her kullanımda aynı; `run_intro` imzası Task 5 pin testi + (b)/(c) bloklarında aynı; `intro_needed(global, &index_content)` Task 2 tanımı = Task 5 çağrısı (`index_content` `run.rs`'te zaten scope'ta).
- Bilinen kabuller (spec'te "kenar durumlar"): Suggest yolunda `[GAME]` açılış satırı atlanır; intro system `DEFAULT_TOPIC` ile yüklenir; model marker'ı erken basarsa kabul edilir.
