# Deneme Sınavı Implementation Plan (Roadmap #7)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Ön-koşul:** `2026-08-15-progress-stats` planı MERGE EDİLMİŞ olmalı (sıralı zincir — main.rs/progress.rs çakışması). Spec: `docs/superpowers/specs/2026-08-15-mock-exam-design.md` — önce oku.

**Goal:** Hedefli konuda `/exam` → deneme sınavı turu (tek soru, ipucu askıda, skorlu kırılım); hedefsizde kapı mesajı; skor kapanışta ölçüm günlüğüne; GOAL.md sınav kuralları; v0.16.0.

**Architecture:** Saf parçalar main.rs'te (`is_exam_command`, `topic_has_goal`) + progress.rs'te `exam_prompt`; iki döngüye intercept-enjeksiyon kolu (/watch deseni + opening enjeksiyon mekaniği); GOAL.md embedded kural bölümü.

**Tech Stack:** Rust, yeni bağımlılık YOK.

## Global Constraints

- Prompt/UI İngilizce. Hedef kontrol önceliği brain.rs'teki approach-önceliğiyle BİREBİR aynı (proje override > global).
- `/exam` hedefsiz konuda LLM'e GİTMEZ.
- Her task sonunda commit (Türkçe mesaj) + push + `cargo test` yeşil.

---

### Task 1: Saf parçalar — `is_exam_command` + `exam_prompt` + `topic_has_goal`

**Files:** Modify: `src/main.rs` (komut + hedef kontrolü), `src/progress.rs` (`exam_prompt`) · testler in-module

- [ ] **Step 1: Write the failing tests**

```rust
// main.rs:
#[test]
fn is_exam_command_exact_only() {
    assert!(is_exam_command("/exam"));
    assert!(is_exam_command("  /exam  "));
    assert!(!is_exam_command("/exam now"));
    assert!(!is_exam_command("exam"));
    assert!(!is_exam_command("/examx"));
}

#[test]
fn topic_has_goal_override_priority() {
    let base = std::env::temp_dir().join(format!("usta_exam_goal_{}", std::process::id()));
    let _ = std::fs::remove_dir_all(&base);
    let project = base.join("proj");
    let global = base.join("global");
    std::fs::create_dir_all(project.join(".usta/approaches")).unwrap();
    std::fs::create_dir_all(global.join("approaches")).unwrap();

    // yalnız global hedefli → true
    std::fs::write(global.join("approaches/rust.md"), "yaklaşım\n## Hedef\nsınav").unwrap();
    assert!(topic_has_goal(&project, &global, "rust"));

    // override VAR ama hedefsiz → override kazanır → false
    std::fs::write(project.join(".usta/approaches/rust.md"), "yaklaşım hedefsiz").unwrap();
    assert!(!topic_has_goal(&project, &global, "rust"));

    // override hedefli → true
    std::fs::write(project.join(".usta/approaches/rust.md"), "## Hedef\nCEFR B2").unwrap();
    assert!(topic_has_goal(&project, &global, "rust"));

    // hiç dosya yok → false
    assert!(!topic_has_goal(&project, &global, "linux"));

    let _ = std::fs::remove_dir_all(&base);
}

// progress.rs:
#[test]
fn exam_prompt_defines_exam_contract() {
    let s = exam_prompt("almanca");
    assert!(s.contains("EXAM MODE"));
    assert!(s.contains("almanca"));
    assert!(s.contains("ONE question at a time"));
    assert!(s.contains("SUSPENDED"));
    assert!(s.contains("recorded at session close"));
    assert!(s.contains("stop the exam"));
}
```

- [ ] **Step 2:** Run: `cargo test exam` → derleme hatası

- [ ] **Step 3: Implement**

```rust
// main.rs — help::is_help_command deseni:
pub(crate) fn is_exam_command(line: &str) -> bool {
    line.trim() == "/exam"
}

/// Does this topic have a goal (## Hedef)? Same approach-file priority as
/// brain.rs GOAL loading: project override wins over global — keep in sync.
pub(crate) fn topic_has_goal(project_root: &Path, global: &Path, topic: &str) -> bool {
    let override_path = progress::approach_path(project_root, topic);
    let path = if override_path.exists() {
        override_path
    } else {
        global.join("approaches").join(format!("{topic}.md"))
    };
    std::fs::read_to_string(path)
        .map(|c| c.contains("## Hedef"))
        .unwrap_or(false)
}
```

`progress.rs` — `exam_prompt(topic)`: spec'teki `[EXAM MODE — MOCK EXAM]` metni birebir (format string, `{topic}` gömülü).

- [ ] **Step 4:** Run: `cargo test exam` → PASS

- [ ] **Step 5: Commit + push**

```bash
git add src/main.rs src/progress.rs
git commit -m "sınav: is_exam_command + topic_has_goal + exam_prompt"
git push
```

---

### Task 2: Döngü entegrasyonu (TUI + plain)

**Files:** Modify: `src/tui/run.rs` (Submit kolundaki `/watch`-`/help` intercept bloğu), `src/main.rs` (`run_plain_loop` Line kolu)

- [ ] **Step 1: Implement — TUI**

`/help` intercept'inin yanına (koda bak — `global` ve `project_root` bu scope'ta; değilse parametre zincirinden getir):

```rust
                        if crate::is_exam_command(&line) {
                            page_user_echo(&mut tui, line.trim())?;
                            if !crate::topic_has_goal(project_root, &global, &topic) {
                                page_notice(&mut tui, "no goal set for this topic — /exam needs a goal (exam/certificate); set one in the introduction")?;
                                continue;
                            }
                            let exam = progress::exam_prompt(&topic);
                            session.push_user(&exam);
                            recorder.user(&exam);
                            // normal ask akışı — mevcut kullanıcı-mesajı gönderim yolunu izle
                            /* mevcut Submit-sonrası ask_live çağrısının aynısı — koda bak,
                               oradaki akışı yeniden kullan (yanıt yazdırma + show marker + token güncelleme) */
                            continue;
                        }
```

DİKKAT: Submit kolunda normal mesaj akışı zaten `session.push_user(&line)` + ask yapıyorsa, en temiz yol: `line`'ı exam_prompt ile DEĞİŞTİRİP normal akışa bırakmak (`let line = exam;` + echo'yu `/exam` olarak bas) — kod tekrarı yerine bunu tercih et; hangisi mevcut yapıya oturuyorsa onu uygula, sapmayı raporla.

- [ ] **Step 2: Implement — plain** (`run_plain_loop` Line kolu, `parse_watch_command`/`is_help_command` yanı): aynı mantık — hedefsiz → `println!` notice + `ready_tx.send(())` + `continue`; hedefli → girilen satır yerine `exam_prompt` gönderilir (kullanıcı echo'su zaten rustyline'da).

- [ ] **Step 3:** `cargo build && cargo test` → PASS. Commit + push:

```bash
git add src/tui/run.rs src/main.rs
git commit -m "sınav: /exam intercept — hedef kapısı + exam turu enjeksiyonu (TUI + plain)"
git push
```

---

### Task 3: Kapanış kuralı + GOAL.md + help

**Files:** Modify: `src/progress.rs` (`closing_prompt`), `GOAL.md`, `src/help.rs`

- [ ] **Step 1: Write the failing tests**

```rust
// progress.rs:
#[test]
fn closing_prompt_records_mock_exam() {
    let s = closing_prompt("rust", None, None, None, None, None, None);
    assert!(s.contains("mock exam"));
}
// help.rs testine: assert!(help_text().contains("/exam"));
```

- [ ] **Step 2:** Run → FAIL

- [ ] **Step 3: Implement**

- `closing_prompt` `## Hedef Durumu` kural cümlesine ek: `If a mock exam (/exam) ran this session, append its result to the measurement log (date | mock exam | score) and record weak items as gaps.`
- `GOAL.md` sonuna `## Mock Exams` bölümü:

```markdown
## Mock Exams

`/exam` runs a mock exam built from the curriculum map, in the goal's format.

- One question at a time; wait for the answer. During the exam the hint ladder and all teaching are SUSPENDED — this is a rehearsal, not a lesson.
- Weight questions toward items not yet `oturdu` and known gaps.
- State question count and time budget up front (from the goal's format).
- Scoring: against the goal's passing threshold. Give a per-map-item breakdown; weak items become gap candidates and return to normal teaching AFTER the exam.
- 'stop the exam' ends early — score what was answered.
- The result goes into `## Hedef Durumu`'s measurement log at session close. Mock exams are the strongest retrieval practice — suggest one when the goal date approaches or the map nears completion.
```

- `help.rs` In-session commands: `/exam            goal mode: timed mock exam from your map` (+ mevcut help testi güncelle).

- [ ] **Step 4:** `cargo test` → PASS. Commit + push:

```bash
git add src/progress.rs GOAL.md src/help.rs
git commit -m "sınav: kapanış kayıt kuralı + GOAL.md Mock Exams + /help satırı"
git push
```

---

### Task 4: Docs + v0.16.0

**Files:** `SPEC.md`, `README.md`, `docs/ROADMAP.md`, `Cargo.toml`(+lock), sürüm testi

- [ ] **Step 1:** SPEC yeni § (v0.16): /exam kapısı, enjeksiyon, askıya alınan ipucu merdiveni, ölçüm günlüğü kaydı.
- [ ] **Step 2:** README (İngilizce) — Pedagogy satırına ek veya yeni satır: `/exam: in goal mode, a mock exam built from your map — no hints, real scoring against your target, weak spots become gaps.`
- [ ] **Step 3:** ROADMAP #7 `✅ tamamlandı (2026-08-15)` + Tamamlananlar.
- [ ] **Step 4:** Cargo `0.16.0`; sürüm testi güncelle; `cargo build`.
- [ ] **Step 5:** Verify: `cargo test` PASS · clippy yeni uyarı 0 · `cargo install --path .`.

```bash
git add SPEC.md README.md docs/ROADMAP.md Cargo.toml Cargo.lock src/
git commit -m "sınav: SPEC + README + roadmap #7 kapandı — v0.16.0"
git push
git tag v0.16.0 && git push --tags
```

- [ ] **Step 6 (elle doğrulama — ATLA, Anil koşacak):** hedefli konuda `/exam` → tek-soru akışı, ipucu yok, skor + kırılım; hedefsiz konuda kapı mesajı; kapanışta ölçüm günlüğünde `mock exam` satırı.
